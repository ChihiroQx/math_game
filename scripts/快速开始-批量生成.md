# 🚀 快速开始：批量生成Sprite Atlas

## 📋 步骤1：找到TexturePacker安装路径

### 方法A：通过开始菜单
1. 在Windows开始菜单搜索 "TexturePacker"
2. 右键点击 → "打开文件位置"
3. 找到 `TexturePacker.exe` 的完整路径

### 方法B：常见安装路径
- `C:\Program Files\CodeAndWeb\TexturePacker\bin\TexturePacker.exe`
- `C:\Program Files (x86)\CodeAndWeb\TexturePacker\bin\TexturePacker.exe`

---

## 📋 步骤2：修改脚本路径

### 使用批处理脚本（.bat）

1. 打开 `scripts/batch-create-atlas.bat`
2. 找到第9行：
   ```batch
   set TEXTPACKER_PATH="C:\Program Files\CodeAndWeb\TexturePacker\bin\TexturePacker.exe"
   ```
3. 修改为您的实际路径（保留引号）

### 使用PowerShell脚本（.ps1）

1. 打开 `scripts/batch-create-atlas.ps1`
2. 找到第12行：
   ```powershell
   $TexturePackerPath = "C:\Program Files\CodeAndWeb\TexturePacker\bin\TexturePacker.exe"
   ```
3. 修改为您的实际路径

---

## 📋 步骤3：运行脚本

### 方法A：双击运行（.bat）
```bash
# 直接双击 batch-create-atlas.bat
```

### 方法B：命令行运行
```bash
cd MathGame/scripts
batch-create-atlas.bat
```

### 方法C：PowerShell运行
```powershell
cd MathGame/scripts
powershell -ExecutionPolicy Bypass -File batch-create-atlas.ps1
```

---

## 📋 步骤4：检查生成结果

脚本会在 `assets/res-atlas/` 目录生成：

```
assets/res-atlas/
├── player/
│   ├── 307/
│   │   ├── mage_307.png    ← 图集大图
│   │   └── mage_307.json   ← 帧配置
│   ├── 119/
│   │   ├── mage_119.png
│   │   └── mage_119.json
│   └── ...
└── monster/
    ├── monster1.png
    ├── monster1.json
    └── ...
```

---

## ⚠️ 如果脚本报错

### 错误1：找不到TexturePacker
**解决**：检查路径是否正确，确保路径用引号括起来

### 错误2：JSON格式不对
**解决**：可能需要调整 `--format` 参数
- 尝试改为 `--format json-hash`
- 或使用TexturePacker GUI生成一个测试文件，查看正确格式

### 错误3：权限问题
**解决**：以管理员身份运行脚本

---

## 🎯 方式2：使用TexturePacker GUI批量处理

如果您更喜欢使用GUI界面：

### 步骤1：创建项目模板

1. 打开TexturePacker
2. 添加一个角色的所有图片（如 `307/` 文件夹）
3. 设置参数：
   - **Data format**: JSON (Phaser) 或 JSON (Array)
   - **Texture format**: PNG
   - **Max size**: 2048×2048
   - **Algorithm**: MaxRects
4. 保存项目为 `character-template.tps`

### 步骤2：批量处理

1. 在TexturePacker中：
   - 文件 → 批量处理
   - 添加所有角色和怪物文件夹
   - 使用模板设置
   - 设置输出路径
2. 点击"开始"批量生成

---

## ✅ 生成完成后

1. **验证文件**：检查生成的PNG和JSON文件
2. **测试格式**：打开JSON文件，确认格式正确
3. **修改代码**：我会帮您修改代码以支持atlas加载
4. **替换资源**：将atlas文件复制到正确位置

---

## 💡 提示

- 脚本会自动为所有6个角色和8个怪物生成atlas
- 如果某个角色/怪物生成失败，会显示错误信息
- 生成的文件会保存在 `assets/res-atlas/` 目录
- 原始文件不会被删除，可以安全测试

---

**运行脚本后告诉我结果，我会帮您修改代码！** 🚀

