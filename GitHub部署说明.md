# 📦 将 dist 推送到 GitHub

## 为什么要推送 dist？

- ✅ 方便直接使用构建好的文件
- ✅ 可以使用 GitHub Pages 直接部署
- ✅ 无需每次都重新构建
- ✅ 其他人可以直接下载运行

---

## 🚀 推送方法

### 方法1：使用自动脚本（推荐）

双击运行：
```
build-and-push.bat
```

脚本会自动：
1. ✅ 构建游戏（生成 dist）
2. ✅ 添加所有文件（包括 dist）
3. ✅ 提交到 Git
4. ✅ 推送到 GitHub

---

### 方法2：手动推送

```bash
# 1. 构建游戏
npm run build

# 2. 添加所有文件
git add .
git add dist -f

# 3. 提交
git commit -m "包含构建文件"

# 4. 推送
git push origin main
```

---

## 🌐 使用 GitHub Pages 部署

推送 dist 后，可以直接使用 GitHub Pages：

### 步骤：

1. **进入仓库设置**
   - 访问 https://github.com/ChihiroQx/math_game
   - 点击 **Settings**

2. **启用 GitHub Pages**
   - 左侧菜单 → **Pages**
   - Source: 选择 **main** 分支
   - Folder: 选择 **/dist**
   - 点击 **Save**

3. **获取链接**
   - 等待1-2分钟
   - 访问：`https://chihiroqx.github.io/math_game/`

---

## 📊 仓库结构

推送后的 GitHub 仓库包含：

```
math_game/
├── src/              # 源代码
├── assets/           # 游戏资源
├── dist/             # 构建文件 ⭐ 新增
│   ├── index.html
│   ├── bundle.js
│   └── assets/
├── package.json
├── webpack.config.js
└── README.md
```

---

## 🔄 更新流程

每次修改代码后：

```
修改代码
    ↓
运行 build-and-push.bat
    ↓
自动构建 + 推送
    ↓
GitHub Pages 自动更新
```

---

## ⚠️ 注意事项

### dist 文件夹较大

- dist 包含所有构建和资源文件
- 可能会增加仓库大小
- 建议：定期清理不需要的构建文件

### Git LFS（可选）

如果资源文件特别大（>100MB），考虑使用 Git LFS：

```bash
git lfs install
git lfs track "*.png"
git lfs track "*.mp3"
```

---

## 🆚 两种部署方式对比

### GitHub Pages（使用 dist）
- ✅ 简单直接
- ✅ GitHub 原生支持
- ⚠️ 国内访问较慢
- ✅ 完全免费

### Vercel（自动构建）
- ✅ 速度快
- ✅ 自动构建
- ✅ 全球 CDN
- ✅ 国内访问好

---

## 💡 推荐方案

**同时使用两种方式：**

1. **推送完整代码（含 dist）到 GitHub**
   - 使用 `build-and-push.bat`
   
2. **Vercel 连接 GitHub 自动部署**
   - 每次推送自动更新
   
3. **GitHub Pages 作为备用**
   - 稳定的备份访问方式

---

## 📱 访问地址

推送成功后，游戏可以通过以下方式访问：

1. **GitHub Pages**: `https://chihiroqx.github.io/math_game/`
2. **Vercel**: `https://math-game-xxx.vercel.app/`
3. **下载运行**: 下载 dist 文件夹，直接打开 index.html

---

**准备好了吗？运行 `build-and-push.bat` 开始推送吧！** 🚀

